apiVersion: v1
kind: Service
metadata:
  name: ray-dashboard
  namespace: dani
spec:
  type: NodePort
  selector:
    ray.io/node-type: head
    ray.io/cluster: ray-cluster
  ports:
    - name: dashboard
      port: 8265
      targetPort: 8265
