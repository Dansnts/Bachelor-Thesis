[pytest]
# Run from the repo root with:  .venv/bin/python -m pytest tests/unit
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    # Starlette's TestClient warns about httpx; irrelevant to these tests.
    ignore::DeprecationWarning
